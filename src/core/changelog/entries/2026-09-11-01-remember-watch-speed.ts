import type { ChangelogEntry } from "../types.js";

const entry: ChangelogEntry = {
  date: "2026-09-11",
  title: "The match viewer remembers your speed",
  items: [
    "Every match you watched used to start back at 1x, so if you like half speed or 4x you had to pick it again each time. Now the viewer opens at whatever speed you used last, whether it's a live match or a rewatch. The setting lives in your browser rather than in your save, so it carries across every league you play.",
  ],
};

export default entry;
