import type { ChangelogEntry } from "../types.js";

const entry: ChangelogEntry = {
  date: "2026-09-10",
  title: "Watch a match at half speed",
  items: [
    "The live match viewer had 1x, 2x and 4x. At 1x a match minute goes past every 800 milliseconds, which is fine for following the score and quick enough that a busy spell can be over before you've finished reading it. I've added a 0.5x button, on a live match and on a rewatch of an old one.",
  ],
};

export default entry;
